package security.apps

/**
 * Phase 1 — Installed Applications Scanner.
 * Reads installed app metadata via PackageManager (packageName, versionName,
 * installerPackage, targetSdk, minSdk, debuggable, isSystemApp).
 * Returns raw, JSON-serializable findings only — no risk judgment here.
 */
class AppScanner {
    // TODO: implement using PackageManager
}
