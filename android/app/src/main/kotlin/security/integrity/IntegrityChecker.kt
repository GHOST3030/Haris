package security.integrity

/**
 * Phase 2 — Root indicators (su binary, Magisk indicators, root management apps).
 * A signal for the Risk Engine — never a standalone verdict.
 */
class IntegrityChecker {
    // TODO: implement
}
