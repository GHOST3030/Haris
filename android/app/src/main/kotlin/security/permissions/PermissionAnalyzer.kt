package security.permissions

/**
 * Phase 1 — Extracts an app's requested permissions and classifies them
 * as dangerous / normal / signature.
 */
class PermissionAnalyzer {
    // TODO: implement using PackageManager.getPackageInfo(..., GET_PERMISSIONS)
}
